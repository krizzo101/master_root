# MCP Server Integration Dependencies

# Core MCP Python SDK - Required for all scripts
mcp[cli]>=1.0.0

# Optional: ArXiv MCP server (Python-based)
# Uncomment if using arxiv_mcp_client.py
# arxiv-mcp-server

# Additional Python packages that may be useful
asyncio-mqtt>=0.11.0  # For MQTT transport (if needed)
aiohttp>=3.8.0        # For HTTP transport (if needed)
websockets>=11.0.0    # For WebSocket transport (if needed)

# Development and testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Documentation
sphinx>=5.0.0
sphinx-rtd-theme>=1.2.0

# Note: Node.js packages are installed separately via npm:
# npm install -g firecrawl-mcp
# npm install -g @upstash/context7-mcp
# npm install -g server-sequential-thinking
# npm install -g brave-search-mcp
