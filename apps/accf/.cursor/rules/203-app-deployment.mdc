---
description: "use when DEPLOYING APPLICATIONS to ENFORCE K8S & GITOPS BEST PRACTICES for RELIABLE DELIVERY"
globs:
alwaysApply: false
---

# Deployment Rules

- Build container via multi-stage Dockerfile.
- Push image to registry using semver tag.
- Deploy via ArgoCD; manifests in `infra/k8s/`.
- Use `readinessProbe` and `livenessProbe`.

- Push image to registry using semver tag.
- Deploy via ArgoCD; manifests in `infra/k8s/`.
- Use `readinessProbe` and `livenessProbe`.

- Deploy via ArgoCD; manifests in `infra/k8s/`.
- Use `readinessProbe` and `livenessProbe`.
